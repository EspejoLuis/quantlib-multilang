namespace QuantLibCSharp.Time;

public enum TimeUnit
{
    Days,
    Weeks,
    Months,
    Years
}

/*
By default you have ToString for Enum!
    TimeUnit unit = TimeUnit.Days;
    Console.WriteLine(unit.ToString());
*/